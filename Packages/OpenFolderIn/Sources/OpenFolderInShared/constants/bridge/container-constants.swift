/// Fixed facts about the folder this app and the extension share.
public enum ContainerConstants {

    /// The file written and removed again to prove the folder really can be written to.
    public static let probeFileName = ".write-probe"
}
